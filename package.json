{
  "name": "ml-pdf",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://mhsekhavat.github.io/ml-pdf",
  "devDependencies": {
    "gh-pages": "^0.11.0",
    "react-scripts": "0.6.1"
  },
  "dependencies": {
    "firebase": "^3.4.1",
    "immutable": "^3.8.1",
    "muicss": "^0.9.0",
    "rc-progress": "^2.0.1",
    "react": "^15.3.2",
    "react-d3-components": "^0.6.5",
    "react-dom": "^15.3.2",
    "react-input-range": "^0.9.2",
    "react-joyride": "^1.5.0",
    "react-mixin": "^2.0.2",
    "react-spinjs": "^3.0.0",
    "request": "^2.75.0",
    "underscore": "^1.8.3",
    "victory": "^0.12.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "deploy:clean": "git checkout -B gh-pages && git add -f build && git commit -am \"Rebuild website\" && git filter-branch -f --prune-empty --subdirectory-filter build && git push -f origin gh-pages && git checkout -"
  }
}
